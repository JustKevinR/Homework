import random
pc_list = random.sample(range(100), 10)
print (pc_list)


duplicated = []
list = set()

for elem in pc_list:
    if elem in seen:
        duplicated.append(elem)
    else:
        seen.add(elem)

max1 = max(seen)
list.remove(max1)
max2 = max(list)
print (max2)
print (list[-2])
